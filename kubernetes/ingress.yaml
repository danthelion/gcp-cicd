apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: app
  labels:
    appName: app
spec:
  rules:
  - http:
      paths:
      - path: /
        backend:
          serviceName: app
          servicePort: 80